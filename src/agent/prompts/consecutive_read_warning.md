⚠️ WARNING: You are reading {file_path} for the 3rd time in a row!

This indicates you may be reading the file incrementally in small chunks. This
is inefficient and wastes tokens.

INSTEAD: Read larger chunks at once using a higher num_lines parameter. For
example, use num_lines=100 or num_lines=200 to get more content in a single
read.

The default is only 30 lines. If you need to read a whole file or large section,
increase num_lines appropriately.
