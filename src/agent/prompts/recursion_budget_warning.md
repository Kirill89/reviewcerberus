⚠️ RECURSION BUDGET WARNING ⚠️

You have used {percentage_used}% of your allowed tool calls
({current_step}/{recursion_limit} steps). You have approximately {remaining}
steps remaining.

**ACTION REQUIRED:**

1. Review what you've learned so far from tool results
2. Make any final critical tool calls if needed
3. Start wrapping up your analysis
4. Prepare to return your structured findings

Do NOT waste remaining steps re-reading the same data. Use what you already
have.
